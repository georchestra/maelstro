sources:
  geonetwork_instances: []
  geoserver_instances: []
destinations: {}
db_logging:
  host: "${PGHOST}"
  port: "${PGPORT}"
  login: "${PGUSER}"
  password: "${PGPASSWORD}"
  database: "${PGDATABASE}"
  schema: "maelstro"